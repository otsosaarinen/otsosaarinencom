function ProjectButton({
	title,
	description,
	tags,
	github_link,
}: {
	title: string;
	description: string;
	tags: string;
	github_link: string;
}) {
	return (
		<>
			<div></div>
		</>
	);
}

export default ProjectButton;
